# 부산 내집마련 대시보드 — Claude Code 작업지시서

> **사용 방법:**
> 1. 각 STEP을 Claude Code 채팅창에 **하나씩** 복사해서 붙여넣기
> 2. Claude Code가 완료 메시지를 보내면 ✅ 체크리스트 확인
> 3. 모두 통과하면 다음 STEP으로 진행
> 4. 막히면 에러 메시지를 그대로 Claude Code에 붙여넣기

---

## 프로젝트 개요

**서비스명:** 부산 내집마련 대시보드
**목적:** 부산 거주 개인 사용자를 위한 부동산 정보 자동 수집·분석·알림 웹서비스
**사용자:** 개인 1인 (개인용, 비공개 배포)
**사용 환경:** PC + 스마트폰 (반응형)

---

## 마일스톤 요약

| 버전 | 포함 STEP | 핵심 기능 |
|------|-----------|-----------|
| MVP | STEP 1~5 | 청약·실거래가 수집, 기본 UI |
| v0.2 | STEP 3, 9 | 시세 분석, 개발 호재·정책 뉴스 |
| v0.5 | STEP 6~7 | AI 요약·감성분석, 카카오톡 알림 |
| v0.8 | STEP 7~9 | 이메일 알림, 알림 설정, 대출 계산기 |
| v1.0.0 | STEP 10 | 배포, 통합 테스트, README |

---

## 🔐 API 키 보안 관리 (필독)

### 핵심 원칙: API 키는 코드에 절대 적지 않는다

API 키가 GitHub에 올라가면 자동 스캐너에 의해 수 분 내에 탈취됩니다.
아래 구조를 반드시 지켜주세요.

### 파일 역할 구분

| 파일 | 역할 | GitHub 업로드 |
|------|------|--------------|
| `.env` | 실제 키 보관 | ❌ 절대 금지 |
| `.env.example` | 키 이름만 안내 (값 없음) | ✅ 업로드 |
| `.gitignore` | .env 제외 설정 | ✅ 업로드 |

### .gitignore 필수 내용

프로젝트 루트의 `.gitignore`에 아래 내용이 반드시 포함되어야 합니다:

```gitignore
# 환경변수 (API 키 포함) - 절대 커밋 금지
.env
.env.local
.env.production

# DB 파일
*.db
*.sqlite

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Node
node_modules/
dist/
build/

# 빌드 결과물
backend/static/

# IDE
.DS_Store
.vscode/
.idea/
```

### .env vs .env.example 차이

.env (로컬에만 존재, GitHub에 올리면 안 됨):
```env
ANTHROPIC_API_KEY=sk-ant-api03-실제키값...
KAKAO_ACCESS_TOKEN=실제토큰값...
```

.env.example (GitHub에 올려도 되는 안내 파일):
```env
ANTHROPIC_API_KEY=           # console.anthropic.com 에서 발급
KAKAO_ACCESS_TOKEN=          # developers.kakao.com 에서 발급
```

### Railway 배포 시 키 관리

Railway에서는 .env 파일 대신 **대시보드 환경변수**를 사용합니다.
코드에 키를 넣지 않고, Railway 웹사이트에서 직접 입력하면 됩니다.

Railway 설정 방법:
1. railway.app → 프로젝트 선택
2. Variables 탭 클릭
3. .env 파일 내용을 하나씩 입력
4. 자동으로 서버에 주입됨 (재배포 불필요)

### 키가 실수로 GitHub에 올라갔다면

즉시 해당 서비스에서 키를 폐기하고 새로 발급해야 합니다.
git 히스토리에서 지워도 이미 탈취된 키는 사용 불가합니다.

폐기 방법:
- Anthropic: console.anthropic.com → API Keys → Revoke
- 카카오: developers.kakao.com → 앱 → 앱 키 재설정
- 네이버: developers.naver.com → 앱 → 클라이언트 시크릿 재발급
- Google: myaccount.google.com → 보안 → 앱 비밀번호 삭제

### API 키 취급 순서 (개발 시작부터 배포까지)

**1단계 — 키 발급**
각 서비스 사이트에서 키를 발급받는다. (아래 환경변수 목록의 링크 참고)

**2단계 — .env 파일에만 입력**
Claude Code가 STEP 1에서 `.env.example`을 만들어준다.
그 파일을 복사해서 `.env`를 만들고 실제 키를 입력한다.
```bash
cp .env.example .env
# .env 파일 열어서 키 값 입력
```

**3단계 — 코드 파일에는 절대 키를 적지 않는다**
```python
# ❌ 절대 금지
ANTHROPIC_API_KEY = "sk-ant-api03-..."

# ✅ 올바른 방법
from app.core.config import settings
key = settings.ANTHROPIC_API_KEY   # .env에서 자동으로 불러옴
```

**4단계 — GitHub 업로드 전 반드시 확인 (아래 체크리스트)**

**5단계 — Railway 배포 시 Variables 탭에 직접 입력**
`.env` 파일은 로컬에만 있으므로 Railway에는 따로 입력해야 한다.
railway.app → 프로젝트 → Variables 탭 → `.env` 내용 그대로 복붙

---

### ⚠️ GitHub 배포 전 필수 확인 절차 (반드시 실행)

GitHub에 push하기 전에 아래 명령어를 순서대로 실행하고
모두 통과해야 push할 수 있다.

**Step A. .gitignore에 .env가 있는지 확인**
```bash
cat .gitignore | grep "^\.env"
# 출력 결과에 ".env" 가 보여야 함
# 아무것도 안 나오면 .gitignore에 .env 추가 후 다시 확인
```

**Step B. git이 .env를 추적하고 있지 않은지 확인**
```bash
git status
# 출력 목록에 ".env"가 없어야 함
# 만약 보이면 → 절대 커밋하지 말고 아래 명령 실행:
# echo ".env" >> .gitignore
# git rm --cached .env
```

**Step C. 혹시 과거에 .env를 커밋한 적 없는지 확인**
```bash
git log --all --full-history -- .env
# 아무것도 출력되지 않아야 함 (출력되면 키 즉시 폐기 후 재발급)
```

**Step D. 코드 파일 안에 실제 키 값이 없는지 확인**
```bash
# Windows (PowerShell)
Select-String -Path "backend/**/*.py" -Pattern "sk-ant|Bearer [a-zA-Z0-9]"

# Mac/Linux
grep -r "sk-ant\|AAAA\|AIza" backend/ --include="*.py"
# 아무것도 출력되지 않아야 함
```

**Step E. .env.example에 실제 키 값이 없는지 확인**
```bash
cat .env.example
# 모든 값이 비어있거나 주석만 있어야 함
# 예: ANTHROPIC_API_KEY=   (O)
# 예: ANTHROPIC_API_KEY=sk-ant-...  (X)
```

**모두 통과했다면 push 진행:**
```bash
git add .
git commit -m "feat: 초기 프로젝트 세팅"
git push origin main
```

---

### GitHub 시작 전 체크리스트

```
git init 하기 전에:
  [ ] .gitignore 파일 생성 완료
  [ ] .env 파일이 .gitignore에 포함되어 있음
  [ ] .env.example 파일만 만들어둠 (실제 값 없음)

첫 커밋 전에 (위 Step A~E 모두 실행):
  [ ] Step A: .gitignore에 .env 포함 확인
  [ ] Step B: git status에 .env 없음 확인
  [ ] Step C: 과거 커밋에 .env 없음 확인
  [ ] Step D: 코드 파일 안에 키 값 없음 확인
  [ ] Step E: .env.example에 실제 값 없음 확인
```

---

## 환경변수 목록 (.env)

STEP 1 시작 전에 아래 키들을 미리 발급받아 두세요.

```env
# 공공 API
MOLIT_API_KEY=          # 국토부 실거래가 API (data.go.kr)
APPLYHOME_API_KEY=      # 청약홈 API (applyhome.co.kr)
LH_API_KEY=             # LH청약센터 API (data.go.kr)

# 네이버 API (developers.naver.com)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# AI (console.anthropic.com)
ANTHROPIC_API_KEY=

# 카카오 (developers.kakao.com)
KAKAO_ACCESS_TOKEN=

# 이메일
GMAIL_USER=             # Gmail 주소
GMAIL_APP_PASSWORD=     # Gmail 앱 비밀번호 (2단계 인증 후 발급)

# DB
DATABASE_URL=sqlite:///./dashboard.db

# 앱 설정
TZ=Asia/Seoul
USE_MOCK=true           # API 키 없을 때 목업 데이터 사용

# 보안
ADMIN_TOKEN=            # 어드민 엔드포인트 인증 토큰 (openssl rand -hex 32 로 생성)
ALLOWED_ORIGINS=http://localhost:5173  # 배포 시 실제 도메인으로 변경 (예: https://my-app.railway.app)
```

---

## [STEP 1] 프로젝트 초기 세팅

```
아래 내용대로 프로젝트를 초기화해줘.

프로젝트명: busan-home-dashboard
백엔드: Python 3.11 + FastAPI
프론트엔드: React 18 + Vite + Tailwind CSS
DB: SQLite (개발용)
패키지 매니저: pip (백엔드), npm (프론트)

생성할 폴더 구조:

busan-home-dashboard/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   ├── services/
│   │   ├── models/
│   │   ├── scheduler/
│   │   └── core/
│   ├── main.py
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   └── hooks/
│   └── package.json
├── .env.example
├── CLAUDE.md
└── README.md

requirements.txt 내용:
fastapi==0.111.0
uvicorn==0.30.0
sqlalchemy==2.0.30
alembic==1.13.1
apscheduler==3.10.4
httpx==0.27.0
beautifulsoup4==4.12.3
python-dotenv==1.0.1
anthropic==0.28.0
python-multipart==0.0.9
aiosqlite==0.20.0
aiosmtplib==3.0.1

추가 작업:
1. 각 폴더에 __init__.py 생성
2. .env.example 파일 생성 (환경변수 목록 내용으로)
3. backend/app/core/config.py 에 환경변수 로드 설정 (pydantic-settings 사용)
4. backend/app/core/database.py 에 SQLAlchemy 비동기 세션 설정
5. backend/main.py 에 FastAPI 앱 기본 설정 (CORS 전체 허용, lifespan 이벤트 포함)
6. frontend를 Vite + React + Tailwind로 초기화
7. frontend/src/hooks/useApi.js 에 axios 기반 useFetch 커스텀 훅 작성

완료 후 아래 명령어로 정상 동작 확인:
- cd backend && pip install -r requirements.txt
- cd frontend && npm install
- 백엔드: uvicorn main:app --reload → http://localhost:8000 접속
- 프론트: npm run dev → http://localhost:5173 접속
```

✅ STEP 1 완료 체크리스트
- [ ] 폴더 구조 생성 완료
- [ ] .gitignore 생성 확인 (.env 포함 여부 확인)
- [ ] .env.example 생성 확인 (실제 값 없이 키 이름만)
- [ ] .env 파일 로컬에만 존재하는지 확인
- [ ] pip install 에러 없음
- [ ] npm install 에러 없음
- [ ] 백엔드 8000 포트 실행 확인
- [ ] 프론트 5173 포트 실행 확인

다음: STEP 2로 이동

---

## [STEP 2] 데이터베이스 모델 정의

```
backend/app/models/models.py 에 SQLAlchemy ORM 모델을 정의해줘.
비동기(async) 세션 기반으로 작성해.

테이블 1: subscriptions (청약·공고)
- id: Integer, PK, autoincrement
- title: String(200), not null
- type: String(20), not null  # "청약"|"줍줍"|"행복주택"|"공공임대"
- location: String(100)
- supply_count: Integer
- price_min: BigInteger
- price_max: BigInteger
- application_start: Date
- application_end: Date
- source_url: String(500)
- created_at: DateTime, default=now
- is_notified: Boolean, default=False

테이블 2: transactions (실거래가)
- id: Integer, PK, autoincrement
- district: String(20)   # 구
- dong: String(30)       # 동
- apt_name: String(100)
- area_sqm: Float
- floor: Integer
- price_won: BigInteger
- deal_date: Date
- build_year: Integer
- created_at: DateTime, default=now

테이블 3: news_reactions (뉴스·반응)
- id: Integer, PK, autoincrement
- title: String(300)
- summary: Text
- source_url: String(500), unique
- source_type: String(20)   # "뉴스"|"블로그"
- sentiment: String(10)     # "positive"|"negative"|"neutral"
- sentiment_score: Float
- published_at: DateTime
- created_at: DateTime, default=now

테이블 4: market_stats (시세 통계)
- id: Integer, PK, autoincrement
- district: String(20)
- avg_price: BigInteger
- avg_jeonse_price: BigInteger
- jeonse_rate: Float
- unsold_count: Integer
- transaction_volume: Integer
- stat_date: Date
- created_at: DateTime, default=now

테이블 5: notifications_log (알림 발송 기록)
- id: Integer, PK, autoincrement
- type: String(10)   # "kakao"|"email"
- content: Text
- sent_at: DateTime, default=now
- success: Boolean

테이블 6: app_settings (앱 설정, 항상 1행만 존재)
- id: Integer, PK (기본값 1)
- kakao_enabled: Boolean, default=True
- email_enabled: Boolean, default=False
- email_address: String(100)
- notify_subscription: Boolean, default=True
- notify_jjupjjup: Boolean, default=True
- notify_happy_house: Boolean, default=True
- notify_public_rental: Boolean, default=True
- notify_policy: Boolean, default=False

추가 작업:
1. backend/app/core/database.py 에 create_all() 초기화 함수 추가
2. backend/main.py lifespan 에 DB 초기화 연결
3. Alembic 초기화 및 alembic/env.py async 방식 설정

완료 후 확인:
- 서버 재시작 시 dashboard.db 자동 생성
- http://localhost:8000/docs Swagger UI 접속 가능
```

✅ STEP 2 완료 체크리스트
- [ ] 6개 테이블 모델 생성 완료
- [ ] dashboard.db 파일 자동 생성 확인
- [ ] Swagger UI 접속 가능

다음: STEP 3으로 이동

---

## [STEP 3] 목업 데이터 및 데이터 수집 서비스

```
API 키 없이도 개발 가능하도록 목업 데이터를 먼저 만들고,
실제 API 연동 서비스도 함께 구현해줘.

파일 1: backend/app/core/mock_data.py
부산 실제 지명과 아파트명 사용. 아래 구조로 각 항목 작성.

MOCK_SUBSCRIPTIONS = 10건
- 청약 4건 (해운대, 수영, 동래, 남구)
- 줍줍 2건
- 행복주택 2건
- 공공임대 2건
- 마감임박(3일 이내) 항목 2건 포함

MOCK_TRANSACTIONS = 30건
- 부산 주요 구 골고루 포함 (해운대, 수영, 동래, 남구, 사하, 북구, 기장)
- 가격 범위: 2억~12억 현실적으로

MOCK_NEWS = 10건
- positive 4건, negative 3건, neutral 3건
- 뉴스 5건, 블로그 5건
- 각 항목에 AI 요약 3줄 포함

MOCK_MARKET_STATS = 부산 16개 구 전체
- 각 구별 현실적인 평균 매매가, 전세가율 설정


파일 2: backend/app/services/subscription_service.py

async def fetch_subscriptions() -> list:
    if settings.USE_MOCK:
        return MOCK_SUBSCRIPTIONS
    
    # 실제 API: 청약홈 오픈API + LH청약센터 API
    # 청약홈: https://www.applyhome.co.kr/ai/aia/selectAPTLttotPblancListForm.do
    # LH: 공공데이터포털 LH 임대주택 분양정보 API
    # 부산 지역 필터: 시도코드=26
    # 결과를 MOCK_SUBSCRIPTIONS와 동일한 dict 구조로 반환
    ...

파일 3: backend/app/services/transaction_service.py

async def fetch_transactions(yearmonth: str = None) -> list:
    if settings.USE_MOCK:
        return MOCK_TRANSACTIONS
    
    # 실제 API: 국토부 아파트매매 실거래 자료
    # endpoint: http://openapi.molit.go.kr/OpenAPI_ToolInstallPackage/service/rest/RTMSOBJSvc/getRTMSDataSvcAptTradeDev
    # 부산 법정동 코드: 26 시작
    # 최초 실행: 최근 3개월치, 이후: 당월만
    ...

파일 4: backend/app/services/news_service.py

async def fetch_news() -> list:
    if settings.USE_MOCK:
        return MOCK_NEWS
    
    # 네이버 검색 API
    # 뉴스: https://openapi.naver.com/v1/search/news.json
    # 블로그: https://openapi.naver.com/v1/search/blog.json
    # 키워드: ["부산 아파트", "부산 청약", "부산 부동산", "부산 재개발", "부산 분양", "부산 전세"]
    # source_url 기준 중복 제거
    ...

파일 5: backend/app/services/market_stats_service.py

async def fetch_market_stats() -> list:
    if settings.USE_MOCK:
        return MOCK_MARKET_STATS
    
    # transactions 테이블에서 구별 집계
    # jeonse_rate = avg_jeonse_price / avg_price
    ...

모든 서비스: async 함수, try/except 필수, 에러 시 [] 반환
```

✅ STEP 3 완료 체크리스트
- [ ] mock_data.py 생성 (현실적인 부산 데이터)
- [ ] 5개 서비스 파일 생성 완료
- [ ] USE_MOCK=true 상태에서 import 에러 없음

다음: STEP 4로 이동

---

## [STEP 4] 스케줄러 설정

```
backend/app/scheduler/tasks.py 에 APScheduler 작업을 설정해줘.

from apscheduler.schedulers.asyncio import AsyncIOScheduler
from apscheduler.triggers.cron import CronTrigger
from zoneinfo import ZoneInfo

KST = ZoneInfo("Asia/Seoul")
scheduler = AsyncIOScheduler(timezone=KST)

작업 1: daily_full_update()
실행: 매일 00:00 KST (cron: hour=0, minute=0)
순서:
  1. fetch_subscriptions() → DB upsert (source_url 기준 중복 제거)
  2. fetch_transactions() → DB insert (신규만)
  3. fetch_news() → DB upsert (source_url 기준)
  4. fetch_market_stats() → DB upsert (district + stat_date 기준)
  5. process_unanalyzed_news() 실행 (AI 감성분석)
  6. 신규 공고 있으면 send_alert() 호출
  7. 완료 로그 출력

작업 2: urgent_check()
실행: 매 6시간 (cron: hour="0,6,12,18", minute=0)
- fetch_subscriptions()에서 type="줍줍"만 수집
- DB에 없는 신규 항목 발견 시 즉시 send_alert() 호출
- 없으면 로그만 출력

backend/main.py lifespan 에 스케줄러 연결:

@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()
    scheduler.start()
    yield
    scheduler.shutdown()

수동 실행 엔드포인트 추가:
POST /api/admin/run-update  # 전체 업데이트 즉시 실행 (테스트용)
POST /api/admin/run-urgent  # 줍줍 긴급 체크 즉시 실행

완료 후 확인:
- 서버 시작 시 "스케줄러 시작" 로그 출력
- POST /api/admin/run-update 호출 시 각 수집 함수 실행 로그 출력
```

✅ STEP 4 완료 체크리스트
- [ ] tasks.py 생성 완료
- [ ] 서버 시작 시 스케줄러 실행 로그 확인
- [ ] /api/admin/run-update 정상 실행 확인

다음: STEP 5로 이동

---

## [STEP 5] FastAPI 라우터 구현

```
backend/app/api/ 에 라우터를 구현해줘.
모든 응답은 Pydantic 스키마로 직렬화하고
페이지네이션은 ?page=1&limit=20 형식 통일.

subscriptions.py:
GET /api/subscriptions
  query: type(선택, "청약"|"줍줍"|"행복주택"|"공공임대"), page, limit
  응답: { total, page, limit, items: [...] }
  정렬: application_end 오름차순 (마감 임박 먼저)

transactions.py:
GET /api/transactions
  query: district(선택), dong(선택), page, limit
  응답: { total, page, limit, items: [...] }
  정렬: deal_date 최신순

news.py:
GET /api/news
  query: sentiment(선택), source_type(선택), page, limit
  응답: { total, page, limit, items: [...] }

GET /api/policy-news
  뉴스 중 아래 키워드 포함 항목 필터링:
  ["정책", "규제", "LTV", "DSR", "특별공급", "세금", "취득세", "금리"]

market.py:
GET /api/market-stats
  가장 최근 stat_date 기준 전체 구 데이터
  응답: { stat_date, districts: [...] }

settings.py:
GET /api/settings
  app_settings 1번 row 반환, 없으면 기본값으로 자동 생성 후 반환

PUT /api/settings
  body: 변경할 필드만 (partial update)
  응답: 업데이트된 전체 settings

notifications.py:
POST /api/notifications/test
  body: { "type": "kakao"|"email"|"both" }
  응답: { "success": bool, "message": str }

POST /api/notifications/run-ai
  summary 없는 뉴스에 AI 분석 즉시 실행
  응답: { "processed_count": int }

admin.py:
POST /api/admin/run-update
POST /api/admin/run-urgent

backend/main.py 에 모든 라우터 prefix="/api" 로 등록.
GET / → {"status": "ok", "service": "부산 내집마련 대시보드"}

완료 후 확인 (USE_MOCK=true 상태):
- GET /api/subscriptions → items 10건 반환
- GET /api/transactions → items 반환
- GET /api/news → items 반환
- GET /api/market-stats → districts 16개 반환
- GET /api/settings → 기본 설정 반환
```

✅ STEP 5 완료 체크리스트
- [ ] 7개 라우터 파일 생성 완료
- [ ] /api/subscriptions 목업 10건 확인
- [ ] /api/market-stats 16개 구 확인
- [ ] Swagger UI 전체 엔드포인트 표시

여기까지 MVP 완성. 다음: STEP 6으로 이동

---

## [STEP 6] Claude API 감성분석·요약 연동

```
backend/app/services/ai_service.py 를 구현해줘.

import anthropic
MODEL = "claude-sonnet-4-20250514"

함수 1: async def analyze_sentiment(title: str, content: str) -> dict
프롬프트:
  "다음 부동산 뉴스를 부산 아파트 매수 희망자 관점에서 분석해줘.
   긍정(호재: 집값 상승, 규제 완화, 개발 계획 등)
   부정(악재: 집값 하락, 규제 강화, 미분양 증가 등)
   중립으로 분류.
   반드시 JSON만 반환 (마크다운 없이):
   {\"label\": \"positive\" or \"negative\" or \"neutral\",
    \"score\": 0.0~1.0,
    \"reason\": \"한 줄 이유\"}"

반환: {"label": str, "score": float, "reason": str}
실패 시: {"label": "neutral", "score": 0.5, "reason": "분석 실패"}

함수 2: async def summarize_articles(articles: list[dict]) -> str
articles: [{"title": str, "content": str}, ...]
프롬프트:
  "부산 부동산 관련 뉴스들을 아파트 매수 희망자 관점에서 3줄로 요약해줘.
   핵심 정보와 시사점만 간결하게. 각 줄은 '-'로 시작."
반환: str (3줄 요약)

함수 3: async def generate_daily_kakao_message(data: dict) -> str
data: {
  "new_subscriptions": int,
  "new_jjupjjup": int,
  "top_news_title": str,
  "avg_price_sample": str,
  "dashboard_url": str
}
프롬프트:
  "아래 데이터로 카카오톡 나에게 보내기용 부동산 일일 요약 메시지를 작성해줘.
   이모지 포함, 200자 이내, 친근한 말투.
   데이터: {data}"
반환: str

함수 4: async def process_unanalyzed_news(db) -> int
- DB에서 sentiment IS NULL 인 뉴스 최대 20건 조회
- 각 항목 analyze_sentiment() 실행
- DB sentiment, sentiment_score, summary 업데이트
- API 호출 간격 0.5초 유지
- 처리 건수 반환

USE_MOCK=true 처리:
- analyze_sentiment: 랜덤하게 positive/negative/neutral 반환
- summarize_articles: "- 목업 요약 1줄\n- 목업 요약 2줄\n- 목업 요약 3줄" 반환
- generate_daily_kakao_message: 하드코딩된 샘플 메시지 반환

완료 후 확인:
- POST /api/notifications/run-ai 호출
- DB news_reactions 테이블 sentiment 컬럼 채워지는지 확인
```

✅ STEP 6 완료 체크리스트
- [ ] ai_service.py 생성 완료
- [ ] USE_MOCK=true 에서 모든 함수 정상 동작
- [ ] run-ai 엔드포인트 호출 후 DB 업데이트 확인

다음: STEP 7로 이동

---

## [STEP 7] 알림 서비스 구현

```
backend/app/services/notification_service.py 를 구현해줘.

클래스 1: KakaoNotifier
카카오 나에게 보내기 API.
POST https://kapi.kakao.com/v2/api/talk/memo/default/send
Authorization: Bearer {KAKAO_ACCESS_TOKEN}

템플릿:
{
  "object_type": "text",
  "text": "{메시지}",
  "link": {
    "web_url": "{DASHBOARD_URL}",
    "mobile_web_url": "{DASHBOARD_URL}"
  },
  "button_title": "대시보드 열기"
}

async def send(self, message: str) -> bool

클래스 2: EmailNotifier
aiosmtplib + Gmail SMTP (smtp.gmail.com:587, STARTTLS).

HTML 이메일 템플릿 (inline CSS):
  헤더: 파란 배경(#3B82F6) "🏠 부산 내집마련 대시보드"
  본문: 알림 내용 (흰 배경)
  버튼: "대시보드 바로가기" (파란 버튼)
  푸터: 회색 소형 텍스트 "개인용 서비스"

async def send(self, subject: str, content: str, to_email: str) -> bool

통합 함수: async def send_alert(alert_type: str, items: list, db) -> None
- alert_type: "subscription"|"jjupjjup"|"daily_summary"
- app_settings 조회 → kakao_enabled, email_enabled 확인
- 카카오: generate_daily_kakao_message()로 메시지 생성 후 발송
- 이메일: HTML 템플릿에 items 내용 채워서 발송
- notifications_log 저장 (성공/실패 모두)

테스트 메시지:
  카카오: "🔔 테스트 알림입니다!\n부산 내집마련 대시보드가 정상 작동 중입니다. ✅"
  이메일 제목: "[부산 내집마련] 테스트 알림"
  이메일 내용: "대시보드 알림 서비스가 정상적으로 설정되었습니다."

USE_MOCK=true 시:
  실제 발송 대신 로그만 출력, success=True 반환

완료 후 확인 (실제 키 입력 후):
  POST /api/notifications/test {"type": "kakao"} → 카톡 수신
  POST /api/notifications/test {"type": "email"} → 이메일 수신
  DB notifications_log 기록 확인
```

✅ STEP 7 완료 체크리스트
- [ ] notification_service.py 생성 완료
- [ ] USE_MOCK 모드에서 로그 출력 확인
- [ ] (선택) 실제 키 입력 후 카카오톡·이메일 수신 확인
- [ ] notifications_log DB 저장 확인

다음: STEP 8로 이동

---

## [STEP 8] 대출 한도 계산기 백엔드

```
DSR·LTV 기반 대출 한도 계산 API를 구현해줘.

파일: backend/app/api/calculator.py

POST /api/calculator/loan
Request:
{
  "property_price": 500000000,
  "property_type": "아파트",       # 아파트|빌라|오피스텔
  "is_first_home": true,
  "is_newlywed": false,
  "region": "투기과열지구",          # 투기과열지구|조정대상지역|일반
  "annual_income": 45000000,
  "monthly_debt": 300000,          # 기존 월 부채 상환액 (없으면 0)
  "loan_years": 30
}

Response:
{
  "ltv_limit_rate": 0.5,
  "ltv_max_amount": 250000000,
  "dsr_max_amount": 180000000,
  "recommended_loan": 180000000,     # 둘 중 작은 값
  "monthly_payment": 850000,         # 원리금균등상환 기준
  "total_interest": 106000000,
  "total_payment": 306000000,
  "dsr_rate": 0.38,
  "applicable_programs": [
    {
      "name": "디딤돌대출",
      "max_amount": 250000000,
      "interest_rate": 0.027,
      "condition": "생애최초, 연소득 7천만원 이하, 5억 이하 주택"
    }
  ],
  "notes": ["투기과열지구 LTV 50% 적용"]
}

LTV 계산 로직 (2024년 기준):
- 투기과열지구: 기본 50%, 생애최초 60%, 신혼부부 60%
- 조정대상지역: 기본 60%, 생애최초 70%, 신혼부부 70%
- 일반지역: 기본 70%, 생애최초 80%
- 오피스텔: 위 비율에서 -10%p

DSR 계산 로직:
- DSR 한도: 40% (은행권 기준)
- 연간 허용 상환액 = 연소득 × 0.40
- 기존 연간 부채 = monthly_debt × 12
- 신규 연간 허용액 = 연간 허용 상환액 - 기존 연간 부채
- 대출 가능 원금 역산 (금리 4.5% 고정 가정, 원리금균등)
- 역산 공식: P = (월상환액 × ((1+r)^n - 1)) / (r × (1+r)^n)
  r = 0.045/12, n = loan_years × 12

정책 대출 해당 여부:
- 디딤돌대출: is_first_home=True, 연소득 <= 7000만, 집값 <= 5억
- 보금자리론: 연소득 <= 7000만, 집값 <= 6억
- 신혼희망타운: is_newlywed=True, 연소득 <= 7000만
- 해당 시 interest_rate, max_amount 함께 반환

POST /api/calculator/monthly-payment
Request:
{
  "loan_amount": 200000000,
  "interest_rate": 0.045,
  "loan_years": 30,
  "payment_type": "원리금균등"   # 원리금균등|원금균등
}
Response:
{
  "monthly_payment": 1013000,
  "total_interest": 164680000,
  "total_payment": 364680000,
  "payment_schedule_summary": {
    "year_1_payment": 1013000,
    "year_10_payment": 1013000,
    "year_20_payment": 1013000
  }
}

완료 후 확인:
- POST /api/calculator/loan 샘플 데이터 → 결과 논리적 확인
  (LTV > DSR이면 recommended_loan = DSR 값인지)
- 생애최초 + 일반지역 → LTV 80% 적용되는지
- 디딤돌대출 조건 맞는 경우 applicable_programs에 포함되는지
```

✅ STEP 8 완료 체크리스트
- [ ] calculator.py 생성 완료
- [ ] LTV·DSR 계산 결과 논리 확인
- [ ] 정책 대출 해당 여부 표시 확인
- [ ] /api/calculator/monthly-payment 정상 응답

다음: STEP 9으로 이동

---

## [STEP 9] 프론트엔드 전체 구현

```
frontend/src/ 에 React + Tailwind 대시보드 UI를 구현해줘.
백엔드 API(localhost:8000)와 연동해서 실제 데이터 표시.

공통 설정:
- 다크 모드 기본: bg-gray-900, text-white
- 메인: #3B82F6(blue-500)
- 긍정: #10B981(emerald-500), 부정: #EF4444(red-500), 중립: #6B7280(gray-500)
- PC(md 이상): 왼쪽 사이드바 240px 고정 + 메인 영역
- 모바일(md 미만): 상단 헤더 + 콘텐츠 + 하단 탭바 5개
- vite.config.js proxy: /api → http://localhost:8000

공통 컴포넌트:

components/Layout.jsx
- 사이드바 메뉴: 홈|청약·공고|실거래가|시세분석|뉴스|정책|계산기|설정
- 모바일 하단 탭: 홈|청약|뉴스|계산기|설정 (5개만)
- 현재 경로 활성화 표시

components/Card.jsx
- bg-gray-800, rounded-xl, p-4
- 제목(굵게), 내용, 선택적 배지 슬롯

components/Badge.jsx
- "청약": blue, "줍줍": purple, "행복주택": green, "공공임대": orange
- "마감임박": red + 깜박임 애니메이션
- "positive": emerald, "negative": red, "neutral": gray

components/LoadingSpinner.jsx - 중앙 스피너
components/ErrorMessage.jsx - 에러 표시

hooks/useApi.js
- useFetch(url, params) : { data, loading, error, refetch }
- axios 기반, 에러 시 console.error

페이지 1: pages/Dashboard.jsx (/)
상단 요약 카드 4개 (2x2 그리드, 모바일):
  - 신규 청약 N건 (파랑)
  - 줍줍 N건 (보라)
  - 부산 평균가 N억 (초록)
  - 전세가율 N% (주황)

최신 청약 공고 카드 3건:
  - 제목, 위치, 공급세대수, 마감일
  - 마감임박 배지 조건부 표시
  - "더보기" 링크 → /subscriptions

오늘의 AI 요약 박스:
  - GET /api/news 첫 번째 항목의 summary 표시
  - 없으면 "오늘의 요약을 불러오는 중입니다." 표시

페이지 2: pages/Subscriptions.jsx (/subscriptions)
탭 버튼: 전체 | 청약 | 줍줍 | 행복주택 | 공공임대
탭 클릭 → query param type= 변경 → API 재요청

카드 1개:
  - 제목 (text-lg font-bold)
  - 위치 | 공급세대수 | 분양가(있으면)
  - 접수기간: YYYY.MM.DD ~ YYYY.MM.DD
  - 마감 3일 이내: "마감임박" 빨간 배지
  - 우측 하단: "공고 원문 보기" 링크 버튼

페이지 3: pages/Transactions.jsx (/transactions)
상단 필터: 구 선택(드롭다운) + 동 입력(텍스트)
테이블: 아파트명 | 면적 | 층 | 거래가 | 거래일
  - 거래가: 억원 단위 표시 (450000000 → 4.5억)
  - 행 클릭 시 강조
페이지네이션 (20건씩, 이전/다음 버튼)

페이지 4: pages/Market.jsx (/market)
구별 평균 매매가 가로 바 차트 (Recharts BarChart):
  - X축: 평균가 (억원)
  - Y축: 구 이름
  - 호버 툴팁: 전세가율, 거래량 표시

전세가율 테이블:
  - 구 | 평균매매가 | 평균전세가 | 전세가율
  - 70% 이상: 노란 배경 강조

미분양 현황 테이블:
  - 구 | 미분양세대수
  - 0이면 "없음" 초록 텍스트

페이지 5: pages/News.jsx (/news)
상단 필터: 전체 | 긍정 | 부정 | 중립

카드 1개:
  - 제목 (클릭 시 원문 새 탭)
  - AI 요약 (3줄, text-gray-400 text-sm)
  - 하단: 감성 배지 + 출처 배지 + 발행일
  - summary 없으면 "AI 분석 대기 중" 회색 텍스트

페이지 6: pages/Policy.jsx (/policy)
상단 현황 카드 3개:
  - 투기과열지구 LTV: 50% (생애최초 60%)
  - DSR 한도: 40%
  - 생애최초 혜택: LTV 최대 80%

정책 뉴스 리스트 (News 페이지와 동일한 카드 형식)

페이지 7: pages/Calculator.jsx (/calculator)
2단 레이아웃 (모바일: 1단):

왼쪽 입력 폼:
  - 집값 입력 (숫자, 콤마 자동 포맷)
  - 주택 유형 선택 버튼 (아파트/빌라/오피스텔)
  - 생애최초 체크박스
  - 신혼부부 체크박스
  - 지역 유형 선택 버튼 (투기과열/조정대상/일반)
  - 연소득 입력
  - 기존 월 부채 입력 (없으면 0)
  - 대출 기간 슬라이더 (10~40년, 기본 30년)
  - "대출 한도 계산하기" 버튼 (파란색, 전체 너비)

오른쪽 결과 (계산 전: 안내 텍스트, 계산 후: 결과 표시):
  - LTV 기준 최대 대출: N억원
  - DSR 기준 최대 대출: N억원
  - ★ 권장 대출 (강조 박스): N억원
  - 예상 월 상환액: N만원
  - 총 이자: N억원
  - 해당 정책 대출 카드 (있으면)
  - 주의사항: "실제 대출은 금융기관 심사에 따라 달라질 수 있습니다."

페이지 8: pages/Settings.jsx (/settings)
GET /api/settings 로 현재 설정 불러오기.

알림 채널 섹션:
  - 카카오톡 ON/OFF 토글 스위치
  - 이메일 ON/OFF 토글 + 활성화 시 이메일 입력 필드 노출

알림 항목 섹션:
  - 새 청약 공고 체크박스
  - 줍줍(무순위) 체크박스 (권장 ON 안내 문구)
  - 행복주택·공공임대 체크박스
  - 정책 변경 뉴스 체크박스

테스트 발송 섹션:
  - "카카오톡 테스트 발송" 버튼
  - "이메일 테스트 발송" 버튼
  - 발송 후 성공/실패 토스트 메시지

하단 "설정 저장" 버튼 → PUT /api/settings

완료 후 확인:
- 8개 페이지 모두 렌더링 (흰 화면 없음)
- 모바일 375px 너비에서 레이아웃 깨짐 없음
- 계산기 입력 후 결과 표시
- 설정 저장 후 새로고침 시 값 유지
- 차트 (시세분석) 정상 렌더링
```

✅ STEP 9 완료 체크리스트
- [ ] 8개 페이지 모두 렌더링 확인
- [ ] 사이드바(PC) / 하단 탭(모바일) 동작
- [ ] 목업 데이터 화면 표시 확인
- [ ] 계산기 결과 표시 확인
- [ ] 설정 저장 동작 확인

다음: STEP 10으로 이동 (최종 배포)

---

## [STEP 10] 배포 및 최종 마무리 (v1.0.0)

```
Railway 배포 설정과 최종 마무리 작업을 진행해줘.

배포 전 준비:
1. backend/app/core/config.py USE_MOCK 기본값 False로 변경
2. frontend/vite.config.js: 빌드 결과를 backend/static/에 출력하도록 설정
3. backend/main.py에 StaticFiles 마운트 추가:
   - /static → backend/static 폴더
   - GET /{full_path:path} → index.html 폴백 (SPA 라우팅)
4. CORS: 개발 중 전체 허용 → 배포 도메인 허용으로 변경 (주석으로 안내)

파일 1: railway.toml (프로젝트 루트)
[build]
builder = "nixpacks"

[deploy]
startCommand = "cd frontend && npm run build && mv dist ../backend/static && cd ../backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

파일 2: .gitignore 최종 정리
- .env
- __pycache__/
- *.pyc
- dashboard.db
- node_modules/
- dist/
- backend/static/

파일 3: README.md 전면 작성
아래 섹션 포함:

# 🏠 부산 내집마련 대시보드

## 주요 기능
(8개 탭 기능 요약)

## 로컬 실행 방법
### 사전 준비
- Python 3.11+
- Node.js 18+
- API 키 발급 (아래 목록 참고)

### 백엔드 실행
cd backend
pip install -r requirements.txt
cp ../.env.example ../.env
# .env 파일에 API 키 입력
uvicorn main:app --reload

### 프론트엔드 실행
cd frontend
npm install
npm run dev

## API 키 발급 방법
| API | 발급처 | 무료 여부 |
|-----|--------|---------|
| 국토부 실거래가 | https://www.data.go.kr | 무료 |
| 청약홈 | https://www.applyhome.co.kr | 무료 |
| LH 공공데이터 | https://www.data.go.kr | 무료 |
| 네이버 검색 | https://developers.naver.com | 무료 |
| 카카오 | https://developers.kakao.com | 무료 |
| Anthropic | https://console.anthropic.com | 유료(종량제) |
| Gmail 앱 비밀번호 | Google 계정 > 보안 > 앱 비밀번호 | 무료 |

## Railway 배포 방법
1. https://railway.app 가입
2. "New Project" → "Deploy from GitHub"
3. 이 저장소 연결
4. "Variables" 탭에서 .env 환경변수 전부 입력
5. 자동 배포 완료 후 도메인 확인

## 환경변수 목록
(전체 목록)

파일 4: 통합 테스트 스크립트 작성
backend/test_integration.py

아래 항목을 순서대로 테스트하는 스크립트:
1. GET / → 200 확인
2. GET /api/subscriptions → items 존재 확인
3. GET /api/transactions → items 존재 확인
4. GET /api/news → items 존재 확인
5. GET /api/market-stats → districts 16개 확인
6. GET /api/settings → 기본 설정 반환 확인
7. POST /api/calculator/loan → 계산 결과 반환 확인
8. POST /api/admin/run-update → 에러 없이 실행 확인

python test_integration.py 실행 시 각 항목 PASS/FAIL 출력.

완료 후 최종 확인:
- python test_integration.py → 8개 항목 모두 PASS
- npm run build → 에러 없음
- railway.toml 파일 존재
- README.md 작성 완료
- .gitignore에 .env 포함 확인
```

✅ STEP 10 완료 체크리스트
- [ ] railway.toml 생성 완료
- [ ] npm run build 에러 없음
- [ ] 백엔드에서 프론트 정적 파일 서빙 확인
- [ ] test_integration.py 8개 항목 모두 PASS
- [ ] README.md 완성

GitHub push 전 보안 확인 (위 "GitHub 배포 전 필수 확인 절차" Step A~E 실행):
- [ ] Step A: .gitignore에 .env 포함 확인
- [ ] Step B: git status에 .env 없음 확인
- [ ] Step C: 과거 커밋에 .env 없음 확인
- [ ] Step D: 코드 파일 안에 키 값 없음 확인
- [ ] Step E: .env.example에 실제 값 없음 확인

Railway 배포:
- [ ] Railway Variables 탭에 환경변수 전부 입력 완료
- [ ] 배포 후 서비스 정상 접속 확인

🎉 v1.0.0 완성! 배포 완료.

---

## 문제 발생 시 대처법

| 증상 | 해결 방법 |
|------|-----------|
| API 키 오류 | .env 확인, USE_MOCK=true 로 임시 전환 |
| DB 오류 | dashboard.db 삭제 후 서버 재시작 |
| CORS 오류 | backend/main.py CORS origins 확인 |
| 카카오 401 | KAKAO_ACCESS_TOKEN 만료 → developers.kakao.com → 내 앱 → 토큰 재발급 후 .env 및 Railway Variables 업데이트 |
| 스케줄러 미실행 | 서버 재시작, lifespan 설정 확인 |
| 프론트 API 연결 실패 | vite.config.js proxy 설정 확인 |
| Railway 빌드 실패 | railway.toml startCommand 경로 확인 |
| .env가 GitHub에 올라감 | 즉시 해당 API 키 전부 폐기 후 재발급 |
| Railway에서 키 인식 못함 | Variables 탭에서 키 이름 오타 확인 |
| 어드민 엔드포인트 403 | X-Admin-Token 헤더 누락 또는 ADMIN_TOKEN 불일치 확인 |
| CORS 오류 (배포 후) | ALLOWED_ORIGINS에 실제 배포 도메인 추가 후 Railway 재배포 |
